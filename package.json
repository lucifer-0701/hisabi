{
    "name": "hisabi",
    "version": "1.0.0",
    "description": "Inventory and Billing SaaS",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "start": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "build": "cd frontend && npm install --include=dev && npm run build && cd ../backend && npm install"
    },
    "devDependencies": {
        "concurrently": "^8.2.2"
    }
}